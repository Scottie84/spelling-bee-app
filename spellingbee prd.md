# PRD — 초등학생용 사진 영단어 퀴즈 (4지선다 · 누적 학습)

**제품명(가칭):** SnapQuiz (SnapVocab Kids)
**한 줄 요약:** 영어 단어가 있는 책을 사진으로 찍으면 **4지선다 객관식 퀴즈**가 무작위로 나오고, 풀었던 단어들이 **저장·누적**되어 다음 사진의 새 단어와 함께 계속 복습할 수 있는 웹페이지.
**상위 문서:** 「웹 MVP PRD」(SnapVocab)에서 갈라져 나온 **어린이 4지선다 버전**.
**버전:** Draft v1.0
**작성일:** 2026-06-13

---

## 1. 목표 (Goal)

초등학생 딸이 **혼자서도 재미있게** 영어 단어를 퀴즈로 공부하도록 한다.

1. 단어책을 사진으로 찍으면 → 자동으로 단어를 읽고 → **4지선다 퀴즈**를 만든다.
2. 모든 문제는 **객관식(보기 4개)** 으로, 타이핑 없이 **터치/클릭**으로 푼다.
3. 공부한 단어는 **저장·누적**되어, 다음에 다른 사진을 찍어도 **이전 단어 + 새 단어를 함께** 퀴즈로 낼 수 있다.

기존 SnapVocab(부모가 입력·여러 유형·주관식 가능)과 달리, 이 버전은 **어린이가 스스로 푸는 4지선다**와 **단어 모음(단어장) 누적**에 초점을 둔다.

---

## 2. 대상 사용자 (Users)

| 사용자 | 역할 | 핵심 니즈 |
|---|---|---|
| 초등학생 딸 | 퀴즈를 푸는 사람 | 쉽고 재미있게, 터치로, 혼자서 |
| 부모 | 사진을 찍고 단어를 확인하는 사람 | 빠른 등록, 잘못 읽힌 단어 수정, 진도 확인 |

- 사용 환경: 주로 **휴대폰/태블릿 세로 화면**, 큰 글씨·큰 버튼.
- 읽기 부담이 적어야 함: 긴 문장보다 **단어·그림·색**으로 안내.

---

## 3. 기존 버전과의 차이 (Scope Diff)

| 항목 | 기존 SnapVocab | **이 버전(어린이용)** |
|---|---|---|
| 문제 형식 | 주관식/객관식 선택 | **항상 4지선다 객관식** |
| 문제 유형 | 유의어/빈칸/뜻 3종 | 뜻↔단어 중심(아래 4.1), 빈칸은 보기형으로 |
| 단어 저장 | 마지막 세트 1개 | **여러 사진 단어를 단어장에 누적** |
| 학습 방식 | 한 세트 풀이 | **전체 단어장에서 무작위 출제 + 복습** |
| 대상 | 부모+학생 | **어린이가 스스로** |

---

## 4. 핵심 기능 (Features)

### 4.1 문제 유형 (모두 4지선다)
사진의 단어 데이터로 만든다. 보기(오답 3개)는 **단어장 안의 다른 단어들**에서 무작위로 뽑는다.

- **유형 A — 뜻 보고 단어 고르기:** 한글/영어 뜻을 보여주고 단어 4개 중 정답 선택.
- **유형 B — 단어 보고 뜻 고르기:** 단어를 보여주고 뜻 4개 중 정답 선택.
- **유형 C — 예문 빈칸 채우기:** 예문에서 단어를 빈칸으로 가리고, 단어 4개 중 정답 선택.
- (선택) **유형 D — 유의어/반의어 고르기:** 유의어·반의어가 있는 단어만, 단어 4개 중 정답 선택.

> 한 퀴즈에 유형이 섞여 나오게 하거나, 부모가 유형을 고를 수 있게 한다(기본: 자동 혼합).

### 4.2 사진 → 단어 자동 등록
- 사진 촬영/선택 → 단어·뜻·예문·유의어/반의어 자동 추출.
- **확인·수정 화면**: 잘못 읽힌 단어 보정(필수). 불확실 단어는 표시.
- 확정하면 **단어장에 추가(누적)**.

### 4.3 단어장 (누적 저장) — 이 버전의 핵심
- 추출·확정한 단어가 **하나의 단어장에 계속 쌓인다.**
- 사진(출처)별로 묶어서 볼 수 있게 **태그/그룹**(예: "Chapter 10", "Chapter 11") 부여 — 사진 등록 시 그룹 이름 입력/자동 제안.
- 단어장 화면: 전체 목록, 그룹별 보기, 단어 검색/삭제/수정.
- **저장 위치(MVP):** 브라우저(localStorage/IndexedDB). 기기 안에 안전 보관, 다시 와도 유지.

### 4.4 퀴즈 만들기 (무작위 출제)
- "퀴즈 시작"을 누르면 **단어장 전체(또는 고른 그룹)** 에서 무작위로 N문제 출제(예: 10문제, 조절 가능).
- **문제 순서·보기 순서 모두 무작위.**
- 출제 범위 선택: **전체 / 특정 그룹 / 이번에 새로 추가한 단어 / 자주 틀린 단어(복습)**.

### 4.5 풀이 & 즉시 피드백 (어린이 친화)
- 보기 버튼을 누르면 **바로 정답/오답 표시**(초록/빨강), 오답이면 정답을 알려줌.
- 다음 문제로 진행. 진행바(예: 3/10).
- 끝나면 **점수 + 칭찬 + 틀린 단어 모아보기**.
- 효과음/별/스티커 같은 **가벼운 보상**(선택, 끌 수 있음).

### 4.6 복습 (선택, 권장)
- 틀린 단어는 **자동으로 "복습 단어"에 표시**되어, 다음 퀴즈에 더 자주 나오게.
- "복습만 풀기" 버튼.

---

## 5. 사용자 흐름 (User Flow)

```
[처음/새 단어 추가]
  사진 찍기 → 단어 자동 추출 → 확인·수정 → 그룹 이름 정하기 → 단어장에 저장(누적)

[공부하기]
  "퀴즈 시작" → 범위 선택(전체/그룹/새 단어/복습) → 문제 수 선택
     → 4지선다 문제 풀기(즉시 피드백) → 결과(점수·칭찬·틀린 단어)
     → "다시 풀기" 또는 "복습만 풀기" 또는 "새 사진 추가"
```

핵심: **사진은 단어를 추가하는 입구**이고, **공부는 누적된 단어장 전체에서** 이뤄진다.

---

## 6. 화면 구성 (Screens)

1. **홈:** 큰 버튼 2개 — "📷 사진으로 단어 추가", "▶ 퀴즈 시작". 단어장 단어 수·그룹 표시.
2. **사진 추가:** 업로드 → 추출 → 확인·수정 → 그룹 이름 → 저장.
3. **단어장:** 전체/그룹별 목록, 단어 수, 수정·삭제, 검색.
4. **퀴즈 설정:** 범위(전체/그룹/새 단어/복습), 문제 수, (선택)유형.
5. **퀴즈 풀이:** 한 문제씩, 보기 4개 버튼, 진행바, 즉시 피드백.
6. **결과:** 점수, 칭찬, 틀린 단어 목록, 다시/복습/추가 버튼.

---

## 7. 데이터 모델 (예시)

```json
{
  "words": [
    {
      "id": "w_001",
      "word": "amount",
      "pos": "n.",
      "meaning": "수량, 양",
      "example": "What is the amount of money that we should each pay?",
      "syn": "quantity",
      "ant": "",
      "group": "Chapter 11",
      "addedAt": "2026-06-13",
      "stats": { "seen": 4, "correct": 3, "wrong": 1 }
    }
  ],
  "settings": { "questionCount": 10, "rewards": true }
}
```
- 단어마다 `group`(출처)과 `stats`(맞힘/틀림) 보관 → 그룹별 출제·복습 가중치에 사용.

---

## 8. 4지선다 보기 생성 규칙 (오답 만들기)

- 정답 외 **보기 3개는 같은 단어장(가능하면 같은 그룹)의 다른 단어**에서 무작위.
- 유형 A/C(정답이 단어): 오답도 **단어**.
- 유형 B(정답이 뜻): 오답도 **다른 단어의 뜻**.
- 단어가 4개 미만이면(보기 부족): 안내하고 단어를 더 추가하도록 유도.
- 보기 순서는 매번 섞는다. 같은 단어가 보기 안에서 중복되지 않게.

---

## 9. 기술 방향 (Technical)

- **구성:** 정적 프론트엔드(HTML/CSS/JS) + 서버리스 함수(`/api/extract`, 사진 인식) + Vercel 배포. (기존 SnapVocab와 동일 구조 재사용)
- **사진 인식:** 멀티모달 LLM이 사진에서 단어 JSON 추출. **API 키는 서버 함수에만.**
- **퀴즈 생성·채점·보기 섞기:** **클라이언트에서 처리**(오프라인 가능, 비용 없음).
- **저장:** 단어장은 **IndexedDB** 권장(localStorage보다 용량·구조에 유리, 단어가 쌓이므로). try/catch로 미지원 환경 대비.
- **백업/이동(선택, 후순위):** 단어장 JSON **내보내기/가져오기** 버튼(기기 바꿔도 이어가기).
- **PWA:** 홈 화면 설치, 오프라인에서 퀴즈 풀이 가능(사진 추가만 네트워크 필요).

> 기존 SnapVocab 프로젝트를 토대로, "단어장 누적 + 4지선다 출제 엔진 + 어린이 UI"를 더하는 형태로 만들 수 있다.

---

## 10. 어린이 사용성·안전 (UX & Safety)

- **큰 글씨·큰 버튼**, 한 화면에 한 가지만. 글보다 색·아이콘 위주.
- 정답/오답은 색 + 아이콘 + (선택)소리로 즉시. 부정적 표현 대신 "다시 해보자".
- 광고·외부 링크·결제 없음. 수집 데이터 최소화(사진/단어 외 개인정보 미수집).
- **API 키 서버 보관**, 사진은 인식 후 저장하지 않음(기본값).
- 외부 공개 시 아동 개인정보 규정(국내 개인정보보호법, 해외 COPPA 등) 검토.

---

## 11. 비기능 요구사항 (Non-functional)

- 모바일 세로 우선, 반응형. 한 손 조작.
- 사진 추가: 업로드→추출 결과까지 체감 10초 이내(로딩 표시).
- 퀴즈 풀이는 네트워크 없이 동작(저장된 단어장 기반).
- 단어가 수백 개로 늘어도 부드럽게 동작.

---

## 12. 완료 기준 (Definition of Done)

- [ ] 사진을 찍으면 단어가 추출되고, 확인·수정 후 단어장에 **누적 저장**된다.
- [ ] "퀴즈 시작"을 누르면 단어장에서 **무작위 4지선다**가 나온다.
- [ ] 보기 3개 오답이 단어장의 다른 단어에서 자동 생성된다.
- [ ] 보기를 누르면 즉시 정답/오답이 표시되고, 끝나면 점수·틀린 단어가 나온다.
- [ ] 앱을 껐다 켜도 단어장이 유지된다.
- [ ] 다른 사진의 새 단어가 기존 단어와 **함께** 출제된다.
- [ ] 휴대폰에서 큰 글씨·큰 버튼으로 편하게 풀 수 있다.
- [ ] API 키가 코드/깃에 없다.

---

## 13. 단계별 우선순위 (Milestones)

- **Phase 1 (MVP):** 사진→추출→확인→단어장 누적 저장 / 4지선다 무작위 출제 / 즉시 피드백·점수 / IndexedDB 저장 / 어린이 UI.
- **Phase 2:** 그룹별 출제, 복습(틀린 단어 가중치), 단어장 내보내기/가져오기, PWA.
- **Phase 3:** 발음(TTS)로 듣고 고르기, 보상/스티커, 진도 리포트(부모용), 다자녀 프로필.

---

## 14. 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| 단어가 적으면 보기 4개를 못 만듦 | 최소 단어 수 안내, 부족 시 추가 유도 / 임시 보기 풀 사용 |
| 흐린 사진 오인식 | 확인·수정 단계 필수, 불확실 표시 |
| 뜻 표기 일관성(한글/영문 혼재) | 추출 시 형식 통일 규칙, 확인 화면에서 수정 |
| 저장 용량/유실 | IndexedDB 사용 + 내보내기 백업 |
| API 키 노출 | 서버 함수 경유, 키 클라이언트 미포함 |
| 과도한 보상으로 산만 | 보상 끄기 옵션, 기본은 절제된 피드백 |
